class Position {
    constructor(id, zone, column, description) {
        this.id = id;
        this.zone = zone;
        this.column = column;
        this.description = description;
    }
}

export default Position;